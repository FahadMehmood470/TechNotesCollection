if (status === 'Submit') { const data = { header: 'Ready to submit?', message: 'You are about to submit this record for review by the Contractor.', isComment: false, closeBtn: 'Cancel', okBtn: 'Submit', status: 'pending_review', }; this.openconfirm(false, data); if (data.okBtn === 'Submit') { const dialogRef = this.dialog.open(PeriodReviewDialogComponent, { width: '250px', data: { ...data } }); dialogRef.afterClosed().subscribe(result => { console.log('The dialog was closed'); }); } }
